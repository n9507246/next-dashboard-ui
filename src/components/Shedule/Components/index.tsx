import Header from "./Header";
import Calendar from "./Calendar";
import СreateContainer from "@/components/ComponentsContainer";

/** Контейнер компонентов Расписания(календарь) */
export default СreateContainer({
    /** Шапка */
    Header, 
    /** Футер */
    Calendar
});